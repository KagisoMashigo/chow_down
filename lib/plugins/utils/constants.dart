const Duration CONNECTION_TIMEOUT_TIME = Duration(milliseconds: 60000);
const Duration RECEIVE_TIMEOUT_TIME = Duration(milliseconds: 20000);
const Duration SEND_TIMEOUT_TIME = Duration(milliseconds: 15000);
